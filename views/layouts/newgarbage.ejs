<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">GORAYA</a> <!-- Replaced "Navbar" with "GORAYA" -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home</a>
        </li>
        <% if (user) { %> <!-- Start of dynamic navigation -->
          <li class="nav-item">
            <a class="nav-link" href="/profile">Profile</a>
          </li>
          <% if (user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/users">Manage Users</a>
            </li>
          <% } %>
          <% if (user.role === 'doctor') { %>
            <li class="nav-item">
              <a class="nav-link" href="/doctor">Doctor</a>
            </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        <% } else { %>
          <button class="button type1" onclick="window.location.href='/register'">
            <span class="btn-txt">Register</span>
          </button>
          <button class="button type1" onclick="window.location.href='/login'">
            <span class="btn-txt">Login</span>
          </button>
          
         
        <% } %> <!-- End of dynamic navigation -->
      </ul>
    </div>
  </nav>
  
  